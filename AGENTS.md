# OpenLegalLexicon 协作要求

- 任何继续、重启、换模型、额度恢复或交接，先从 GitHub 插件读取最新 main 的 HANDOFF.md、COORDINATION.json、PROGRESS.md、WORK_LOG.md，再同步本地副本。对话中的旧进度不能覆盖远端事实。
- 用户要求远端 GitHub 操作使用 GitHub 插件。不要改用浏览器或本地 git push。已有初始化、开发、检查点和下载版发布授权继续有效，常规提交无需反复确认。
- 按 HANDOFF.md 的 45 分钟认领约定协调人工主线写入；每次提交前重新核对 owner 与 claim_id。非强制更新失败后先检查新主线，不复用旧 tree 覆盖并发改动。用户明确接手指令优先。
- 交接说明是项目协作约定，不是自动跨产品共享聊天状态。不得声称某个助手已收到消息、某条自动任务已改动，除非实际执行并核实。
- 保留所有第三方来源、逐条许可、稳定 ID、法域及字段级审核状态。只复核分类时不要把整条译义标为专家审核；法域和法律释义须有具体证据。
- 每个可验证批次更新 README、PROGRESS、WORK_LOG；记录真实测试、CI、自动产物提交和剩余工作。突然中断可从 main 恢复，原始 tree 不是已提交成果。
- 不创建重复的定时任务，不代替用户开启另一个对话；有明确跨对话发送或调度请求时再使用对应工具。
- 公开仓库不放私人聊天链接、任务标识、凭据、私人案件词表或用户本地项目资料。
