# 验证记录与复现

## 已执行

- macOS arm64 / Python 3.12：全量来源、摘要、结构及关系校验通过。
- 18 项测试通过：缺页、变更列、CSV 引号、重复键、数据被修改、重复 ID、悬空关系、许可不匹配、无依据法域、读音边界、隔离条目、导出追溯、TSV 往返、命令退出码与重复构建。
- 两次相同配置构建的所有文件 SHA-256 一致。
- librime 1.11.2：简体、繁体词典真实编译；7 组无空格/单引号分音节的输入返回预期候选，见 `reports/rime-2026-09-11.json`。
- Anki 官方核心 26.8.1：首次导入生成 5,450 条笔记和卡片，再次导入仍为 5,450；逐条比对全部字段和标签无损，见 `reports/anki-2026-09-11.json`。
- GitHub 检查点 218fdbe 的 [CI](https://github.com/chenyuanxi1988/OpenLegalLexicon/actions/runs/34608197073)在 Ubuntu 24.04 的 Python 3.10、3.12 均成功；3.12 还通过真实 Rime 编译与查询。随后新增的 Anki CI 步骤需以对应新提交的运行结果为准。

原始输入法方案用 table_translator 时虽然能编译，却无法查到多音节词。改用 script_translator 后通过。空格是候选选择键，不作为音节间键；单引号用于分音节。

输入法测试创建独立临时用户目录，不读取或更改用户的实际词典与配置。C API 检查代码见 `tests/rime_smoke.c`。

## 复现

```sh
python -m pip install -e '.[dev]'
oll validate
python -m unittest discover -s tests -v
oll build --out build/verify-1
# Linux 上先安装 librime-dev、librime-bin 和 C 编译器：
python tools/verify_rime.py --bundle build/verify-1 --out build/rime-test.json
python -m pip install '.[integration]'
python tools/verify_anki.py --bundle build/verify-1 --out build/anki-test.json
```

macOS 可用已安装输入法自带的 rime_deployer 和 librime 动态库，通过 `--deployer`、`--library` 指定路径；`--include` 指定对应的 rime_api.h 所在目录。头文件取自 Rime 官方仓库，未将第三方头文件复制进本项目。

CI 已配置 Python 3.10 / 3.12 校验，Python 3.12 作真实 Rime 检查；远端是否实际执行成功以 GitHub Actions 记录为准。

## 仍未证明

这些结果证明工程管线与已测输入法行为，不证明 2,730 条词对的法律含义都准确、全部分类都正确，也不证明任意输入法兼容。多数领域分类和所有普通话读音仍属于自动生成。原词表的历史机构、旧法与外国制度需要逐条复核。

跨法域释义补全、私有词库叠加、Qwen3-ASR 真实模型调用与语音识别效果评测仍未完成。尚无已完成的真实模型与音频对照实验，不将计划适配写成已验证。


## 2026-09-12：核心学习层

22 项单元测试通过，包括法条摘要篡改、缺失条号、错误法域、缺少证据的释义等反例。法条抽取工具逐字比对固定原 HTML 中的 101 个所引条文，报告见 `reports/legal-evidence-2026-09-12.json`。

实际 Anki 26.8.1 分别导入完整 5,648 条和核心 198 条笔记/卡片，再次导入数目不变，逐项核对所有字段和标签一致。报告见 `reports/anki-2026-09-12.json` 与 `reports/anki-core-2026-09-12.json`。实际 librime 编译简繁各 2,500 行词典并通过九组候选查询，新增匿名化和个人信息处理者；见 `reports/rime-2026-09-12.json`。这些测试证明结构、格式与数据传递，不代替逐条专家译义复核。


## 2026-09-12：完整证据追溯与统一发布

- 本地 Python 3.12 全部 33 项单元测试通过。新增回归验证后续证据批次变化会改变其摘要，首批摘要不被误当作完整输入；没有首批文件时仍追踪实际加载批次，目录和下载配置不参与法条输入。完整构建测试同时逐项核对四份证据文件摘要。
- 全量构建成功，默认 2,943 条记录、218 条核心学习词条、5,886 条双向学习记录；下载版生成及 `publish_dictionary.py --check` 通过。编审队列共 56 组同形候选、374 个单条高价值候选、1,484 条未分类来源记录。
- 此前已核验 CI 34684881061 成功，下载提交 e9f851e04aae1c565efa50109723bd92f26e6dcd 已包含两批来源注释。上方 18/22 项测试及 Anki 数字是历史批次结果，并非当前全量。
- CI 将词典与编审队列一起生成、一起非强制提交，移除独立队列写入工作流，避免两个同源自动构建互相导致过期发布跳过。源码提交 7b77b794c77997871724ee431ec57b2199add6b2 的 CI 34685523972 三项任务全部成功，Python 3.12 真实 Rime 与 Anki 完整/核心导入及重复导入步骤通过；生成提交 f3a84a23097d63926d1ce880af61cdd2ef4fb707 的 23 个下载文件和编审队列摘要均与本地一致。


## 2026-09-12：来源编审第三批与隔离输出

- 0.1.0.dev4：Python 3.12 本地 37 项测试通过。新增存疑译义阻断学习/英文/Rime 导出、隔离项不冒充已复核锚点、未解决质量问题不因分类完成而消失，以及来源说明/显式审计查询可见性的回归检查。
- 全量校验通过：2,948 条结构化记录、2,952 行来源/项目记录、6 个许可已登记来源。所有原始快照保持不变。
- 构建与下载同步检查通过：默认 2,941 条、5,882 条双向学习记录、218 条核心学习词条、2,713 项英文候选、简繁各 2,610 行 Rime。两个同形中文词仍有其他正常来源，因此 Rime 词面行数不减少，但映射排除了存疑条目 ID。
- 下载 README 现在随构建按统计生成，并纳入下载 SHA256SUMS 与 --check，避免下载页长期停留在旧数据规模。
- 本批源码 e6274eed59e78c89b3c956b3242b3a72c4d1fc5c 的 CI 34702393627 三项任务全部成功，Python 3.12 真实 Anki 完整/核心导入及重复导入、Rime 编译查询通过。生成提交 04cbfbb767ef0af3915139033deed5579499b4c9 已核验：23 个下载文件及编审队列的摘要逐项与本地一致。


## 2026-09-15：内容优先合同批次

- 仅新增/编审数据、法律证据和同步文档；程序仍为 0.1.0.dev4、schema 1.1.0，未扩展 Anki/Rime/ASR 功能。
- 本地 Python 3.12：37 项现有测试全部通过（45.789 秒）；2,996 条记录、3,000 行来源/项目数据、7 个许可来源的全量校验通过。
- 新增 48 条词组均有中文释义、项目英文、译法说明、CN 法域证据、类型与学习提示；无重复核心中文名或 ID。51 个引用法条与原固定 HTML 重抽取一致，并与当日重新下载的同一官方页面逐字一致；完整序列均为 1–1260，详见 reports/legal-evidence-contracts*.json。
- 本地构建通过：2,989 条默认词条、266 条核心词条、5,978 条双向学习记录、2,761 英文候选、简繁各 2,658 行 Rime。本批远端 CI 与下载发布待提交后核验。
- 8 条来源名称记录逐一核对固定原始 HTML/CSV，新增 2 条缩写关系；没有把名称/分类复核升级成专家译义审核。两个原有隔离疑点仍保留。
