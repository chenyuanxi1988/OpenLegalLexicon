# 贡献与更新

请提供原文、译文、法域、用途、来源 URL、条/页/记录定位及可再分发的许可证据。修改已有词条时附 ID，区分原来源错误、导入错误、文字转换、多音字和法律语境差异。

1. 核验来源许可与特殊保留声明，公开可见本身不是再分发依据。
2. 只保存公开词汇，不提交客户、个人、未公开案件或项目资料。私人沟通、账户授权、额度及临时工作登记应放在仓库之外；上传前逐项检查文件，不能只依赖 .gitignore。
3. 修改快照附旧新摘要、计数差异及新增/移除 ID，不能只改摘要掩盖未经审查的内容。
4. 分类、法域、关系和学习说明存于 `data/annotations.json`，保留原词对和依据。
5. 运行 `oll validate`、`python -m unittest discover -s tests -v`，构建全量与相关筛选产物。
6. 修改输入法时运行真实 librime 查询；修改学习导出时验证实际导入和重复导入。
7. 更新 WORK_LOG.md 和 PROGRESS.md，记录证据、结果、缺口及下一步。

代码和文档按 MIT，原创数据注释和编排按 CC BY 4.0，第三方数据保持原许可。不要把机器生成状态改称法律专家审核。

## 更新快照

```sh
python tools/download_sources.py --cache build/source-download-NEW
# 核验新快照总数、许可和差异后：
oll import-snapshot --cache build/source-download-NEW
oll validate
```

重用缓存必须有匹配摘要。每次来源更新选择新目录和日期，并更新来源登记的获取日期、预期总数与快照路径；不要把新数据放进旧日期快照。网页访问量也会改变 HTML 摘要，判断词条更新需比较抽取数据。

结构改版提供迁移工具；当前接受的词条结构版本为 schema_version=1.1.0。原词对变化会改变内容 ID，应记录替换并修复 annotation 和关系。法规废止保留历史记录；来源权属问题记录受影响版本并停止相关数据的新发行。
